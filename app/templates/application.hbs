<NavBar />
<div class="container-fluid">
    <div class="row">
        <div class="col-md-3">
            <ul class="list-group">
                {{#each rootCategories as |category|}}
                <li class="list-group-item">{{category.name}}</li>
                {{/each}}
            </ul>
        </div>
        <div class="col-md-9">
            {{outlet}}
        </div>
    </div>
</div>